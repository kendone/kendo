<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kendo.api.mapper.ApiMapper">

    <!-- RANK SELECT -->
    <select id="selectValidRanks" resultType="rank">
        SELECT
            ID,
            NAME
        FROM sys_rank
        WHERE IS_VALID = 1;
    </select>

    <!-- DEPT RESULTMAP -->
    <resultMap id="deptResultMap" type="dept">
        <id property="id" column="ID" jdbcType="INTEGER"/>
        <result property="name" column="NAME" jdbcType="VARCHAR"/>
    </resultMap>
    <!-- DEPT SELECT -->
    <select id="selectValidDepts" resultType="dept">
        SELECT
            sys_dept.ID,
            sys_dept.NAME
        FROM sys_dept
        WHERE sys_dept.IS_VALID = 1
    </select>
    <select id="selectValidDeptByUserId" parameterType="java.lang.Long" resultMap="deptResultMap">
        SELECT
            sys_dept.ID,
            sys_dept.NAME
        FROM sys_dept
        WHERE sys_dept.IS_VALID = 1
              AND ID IN (
            SELECT sys_user.DEPT_ID
            FROM sys_user
            WHERE sys_user.ID = #{userId,jdbcType=INTEGER}
        )
    </select>

    <!-- POST RESULTMAP -->
    <resultMap id="postResultMap" type="post">
        <id property="id" column="ID" jdbcType="INTEGER"/>
        <result property="name" column="NAME" jdbcType="VARCHAR"/>
    </resultMap>
    <!-- POST SELECT -->
    <select id="selectValidPosts" resultMap="postResultMap">
        SELECT
            ID,
            NAME
        FROM sys_post
        WHERE IS_VALID = 1;
    </select>

    <!-- DICT SELECT -->
    <select id="selectValidDictByType" resultType="dict" parameterType="java.lang.String" useCache="true">
        SELECT
            VALUE,
            TEXT
        FROM sys_dict
        WHERE IS_VALID = 1
              AND TYPE = #{type,jdbcType=VARCHAR}
        ORDER BY SORT ASC
    </select>

    <!-- WINDOW SELECT -->
    <select id="selectValidWindow" resultType="window">
        SELECT
            id,
            name
        FROM sys_window
        WHERE IS_VALID = 1
    </select>
</mapper>